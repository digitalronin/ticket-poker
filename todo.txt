add ticket handling
- validate ticket urls
- redirect to ticket edit page if a ticket was created after updating the team (otherwise, go back to team edit page, with a flash message)
- ticket show page
- display ticket url after creation
- ticket estimates

display team URL after creation

switch to UUID for teams.id
switch to UUID for tickets.id

remove team index view
remove unnecessary ticket views etc.

prevent deleting teams

default points to 1 2 3 5 8 13

use websockets so concurrent users see each others' changes

styling

add funky graphics

show ticket body in a separate (i)frame

deploy

